services:
  - type: web
    name: turno-reale
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run start -- --ip 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 22.13.0
